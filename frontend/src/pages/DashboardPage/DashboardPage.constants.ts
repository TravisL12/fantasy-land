export const DASHBOARD_COPY = {
  back: 'Dashboards',
  loading: 'Loading dashboard…',
  missing: 'That dashboard could not be found.',
  edit: 'Edit',
  /** Labels the prompt block, so the quoted text is read as the user's own. */
  askedFor: 'Asked for',
} as const;
