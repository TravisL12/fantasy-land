import { StyledLink } from './HeaderLink.styles';
import type { HeaderLinkProps } from './HeaderLink.types';

/** A secondary navigation link that sits beside a page title. */
export const HeaderLink = (props: HeaderLinkProps) => <StyledLink {...props} />;
